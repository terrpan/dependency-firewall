apiVersion: ko.build/v1alpha1
kind: Ko

builds:
  - id: firewall
    dir: ./cmd/firewall
    main: .
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s
      - -w
      - -X
      - github.com/danielterry/dependency-firewall/cmd/firewall.ldVersion={{.Env.VERSION}}
      - -X
      - github.com/danielterry/dependency-firewall/cmd/firewall.ldCommit={{.Env.COMMIT}}
      - -X
      - github.com/danielterry/dependency-firewall/cmd/firewall.ldBuildTime={{.Env.BUILD_TIME}}

images:
  - image: dependency-firewall
    build: firewall
    tags:
      - latest
      - '{{.Env.VERSION}}'
